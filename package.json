{
  "name": "partition-queue",
  "version": "1.0.1",
  "description": "Asynchronous function queue with adjustable concurrency and the ability to partition tasks by a key.",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "cover": "istanbul cover _mocha",
    "debug": "mocha --inspect-brk=0.0.0.0:9004",
    "coveralls": "npm run cover -- --report lcovonly && cat ./coverage/lcov.info | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sean256/partition-queue.git"
  },
  "keywords": [
    "queue",
    "partition",
    "job",
    "task",
    "concurrency",
    "concurrent",
    "async",
    "asynchronous"
  ],
  "author": "Sean Harris",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sean256/partition-queue/issues"
  },
  "homepage": "https://github.com/sean256/partition-queue#readme",
  "dependencies": {
    "events": "^2.0.0"
  },
  "devDependencies": {
    "coveralls": "^3.0.0",
    "eslint": "^4.18.1",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.8.0",
    "istanbul": "^0.4.5",
    "mocha": "^5.0.1",
    "mocha-lcov-reporter": "^1.3.0"
  }
}
